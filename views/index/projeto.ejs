<h1><%= projeto.nome %></h1>
<p>Autor(es): <%= projeto.autor  %></p>
<p><%=projeto.cidade  %> - <%=projeto.sigla  %></p>
<input type="button" value="Deletar" id="botaoDelete" onclick="swalDelete('<%=projeto.id %>')">
<input type="button" value="Editar" id="botaoDelete">

<script>
    function swalDelete(id){
        Swal.fire({
             title: 'Tem certeza que quer deletar este projeto?',
             text: "",
             icon: 'warning',
             showCancelButton: true,
             confirmButtonColor: '#3085d6',
             cancelButtonColor: '#d33',
             confirmButtonText: 'Sim, deletar!'
            }).then((result) => {
                if (result.isConfirmed) {
                
                    deletar(id);

                    Swal.fire(
                        'Deleted!',
                        'Your file has been deleted.',
                        'success'
                    )
                }
            })
    }

    async function deletar(id){
        
    }
</script>